rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /scans/{document} {
      // Temporarily allow any logged-in user to read/write to fix permission errors
      allow read, write: if request.auth != null;
    }
  }
}
